apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-of-apps-main.fullname" . }}
  labels:
    {{- include "app-of-apps-main.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-of-apps-main.selectorLabels" . | nindent 4 }}
