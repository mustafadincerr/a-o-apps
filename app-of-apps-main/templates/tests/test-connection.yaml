apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-of-apps-main.fullname" . }}-test-connection"
  labels:
    {{- include "app-of-apps-main.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-of-apps-main.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
